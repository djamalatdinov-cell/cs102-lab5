#include <iostream>
using namespace std;
int main() {
    int n; cin >> n;
     if (n <= 1) {
        cout << "Number is not prime";
    }
    bool p = true;
    for (int i = 2; i * i <= n; i++)
        if (n % i == 0) p = false;
    if (p) cout << "Number is prime";
    else cout << "Number is not prime";

    return 0;
}
